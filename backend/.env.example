# Database connection
DATABASE_URL="postgresql://username:password@localhost:5432/worship_set_manager"

# Server configuration
PORT=3001

# Note: The application will run with mock data if no database is connected
# To use a real database, ensure PostgreSQL is running and update the DATABASE_URL