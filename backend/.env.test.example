# Test Environment Variables
# Copy this file to .env.test and fill in with your test database credentials
# NEVER commit .env.test to version control!

# Test Database URL
# Use a separate database for testing to avoid corrupting production data
DATABASE_URL="postgresql://username:password@localhost:5432/worship_manager_test"

# JWT Configuration (use test values)
JWT_SECRET="test-secret-key-for-testing-only"
JWT_EXPIRES_IN="1h"

# Server Configuration
PORT=3001
NODE_ENV="test"
